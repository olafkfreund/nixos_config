description = "Comprehensive secrets management using Agenix"
prompt = """
Manage NixOS Secrets.

**Action**: "{{args}}" (Default: Status)

**Commands:**
*   **Manage**: Run `./scripts/manage-secrets.sh` (Interactive).
*   **Status**: Check `secrets.nix` existence, agenix availability, and runtime access.
*   **Create**: `manage-secrets.sh create <name>`.
*   **Edit**: `manage-secrets.sh edit <name>`.
*   **Rekey**: `manage-secrets.sh rekey`.
*   **List**: List all `.age` files in `secrets/`.

Execute the requested action.
"""
