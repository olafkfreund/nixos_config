_:
{
  # Spicetify module temporarily disabled due to:
  # 1. Upstream syntax error in spicetify-nix (simpleBeautifulLyrics at extensions.nix:197)
  # 2. base16-schemes has corrupted store paths (2026-01-15 date issue)
  #
  # TODO: Re-enable in Users/common/imports.nix when upstream fixes the issues
  #
  # When re-enabling, restore the full configuration from git history.
}
